# AIC_ProcessData



Step 1: Create `data/zip`

Step 2: Install some libs
```
sudo apt update
sudo apt install ffmpeg -y
sudo apt install unzip
sudo -v; sudo https://rclone.org/install.sh
sudo apt install tmux -y
```

Step 3: rclone config

Step 4: download input file using rclone but I forget, ask gpt later, lol

Step 5: unzip .... 

Step 6: Create conda env

WIP